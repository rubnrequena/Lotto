package controls
{
	import be.aboutme.airserver.Client;
	
	import models.ModelHUB;
	
	public class Afiliado extends Control
	{
		public function Afiliado(cliente:Client, model:ModelHUB)
		{
			super(cliente, model);
		}
	}
}